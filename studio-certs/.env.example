# PostgreSQL (Quick Pic schema, e.g. pg4.sweb.ru). Не коммитить .env с реальными данными.
POSTGRES_URL=postgresql://user:password@host:5432/database

# Optional: notify admin on each certificate sale (Telegram)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_ADMIN_CHAT_ID=

# Admin list: key in header x-admin-key for GET /api/admin/certificates and /admin page
# ADMIN_SECRET=
